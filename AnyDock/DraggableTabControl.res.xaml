<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:wpf="clr-namespace:System.Windows.Controls;assembly=PresentationFramework" 
                    xmlns:local="clr-namespace:AnyDock">
    <!--, diag:PresentationTraceSources.TraceLevel=High-->
    <ControlTemplate x:Key="DraggableTabControlTemplate" TargetType="{x:Type local:DraggableTabControl}">
        <DockPanel ClipToBounds="True" KeyboardNavigation.TabNavigation="Local">
            <local:DraggableTabPanel x:Name="headerPanel" Background="Transparent" Margin="2,2,2,0" IsItemsHost="True"
                                   KeyboardNavigation.TabIndex="1" Panel.ZIndex="1" DockPanel.Dock="{TemplateBinding TabStripPlacement}"/>
            <Border x:Name="contentPanel" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                    KeyboardNavigation.DirectionalNavigation="Contained" KeyboardNavigation.TabIndex="2" KeyboardNavigation.TabNavigation="Local"
                    Background="{TemplateBinding Background}">
                <ContentPresenter x:Name="PART_SelectedContentHost" ContentSource="SelectedContent" 
                                  Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
            </Border>
        </DockPanel>
    </ControlTemplate>
</ResourceDictionary>